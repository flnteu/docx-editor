# Tasks: pro-review-and-custom-nodes

## 1. Core seam

- [x] 1.1 Define `EditorModule` interface in `packages/core/src/contracts/` (reviewModel, commands, displayModes, customNodes contribution points) with null-object defaults
- [x] 1.2 Add `modules` option to `createDocxEditor`; build the module registry and thread it to exec/chrome dispatch points
- [x] 1.3 Gate display modes: editor stays in final-state projection unless a module grants additional `RevisionDisplayMode`s; remove/redirect any free-core API that switches modes
- [x] 1.4 Add `hasReviewContent` derived read to the snapshot (lazy, version-cached, reference-stable)
- [x] 1.5 Add the pro unavailable reason for review slots via `can` refusals (chrome disables through existing plumbing). Reason is engine English like every other refusal reason — no i18n key, matching precedent
- [x] 1.6 Tests: no-module editor renders final state for a tracked-changes fixture, round-trips losslessly, `hasReviewContent` true, review slots disabled with `pro` reason

## 2. Pro package scaffolding

- [x] 2.1 Create `packages/pro` (`@docx-editor.dev/pro`): package.json (`LicenseRef-EigenPal-Pro-Evaluation-1.0`, EigenPal author, peer-deps core + optional react), typecheck config. NOTE: `private: true` for now — publishing is deferred branch-wide with the other adapters
- [x] 2.2 Write commercial `LICENSE.md`; add package to the fixed changeset group and workspace tooling (typecheck, test, api:extract targets)
- [x] 2.3 Accept optional `licenseKey` on pro entry points (stored, unvalidated in v1 — honor system); no warning, no banner, never a network call
- [x] 2.4 Tests: module without key is fully functional and silent; no licensing network traffic

## 3. Review module lift

- [x] 3.1 Move `review-model.ts` and `comment-anchors.ts` from `packages/core/src/layout/` to `packages/pro`; keep `ReviewItem`/anchor types in core `contracts/`
- [x] 3.2 Replace core call sites (`tree-session.ts` `collectReviewItems`, layout barrel exports) with module-registry lookups and null-object defaults
- [x] 3.3 Review commands stay as engine glue over store ops (single write path) but are unreachable without a registered module — pro-reason refusals at can/exec; spec updated to match this cut
- [x] 3.4 Implement `reviewModule(options)` assembling display modes + review model + commands; wire license state
- [x] 3.5 Move `DocxEditorReview.tsx` + `useReview.ts` from `packages/react` to the pro React entry; wire review slots on mount (no banner — licensing is silent honor-system per 2.3)
- [x] 3.6 Review strings already live under the `review.*` namespace in the shared en.json pipeline; pro consumes them via the react locale binding — verified `i18n:validate` green, no key moves needed
- [x] 3.7 Tests: accept/reject/reply round-trip fixtures (semantic digest on untouched content), pane wires slots, published-artifact check that free packages contain no review implementation

## 4. Custom nodes

- [x] 4.1 Verified already-landed: inline SDTs are typed `contentControl` nodes with UTF-16 affinity (`contentControlAtOf`), value/remove commands, and painted literal content; no new modeling needed for recognition
- [x] 4.2 `defineCustomNode` definition shape + tag codec (encode refuses the 64-char cap; decode guards prototype pollution). REMAINING: customXml data-part escape hatch + `sdtLocked` default land with the write side (4.4)
- [x] 4.3 `recognizeCustomNodes` tag-prefix pass (tolerant of Word-demoted generic `sdtPr`); `fromDocx` sees attrs + literal text, null vetoes to literal rendering
- [x] 4.4 Write side v1: `insertInlineContentControl` TreeDocOp (paragraph-level split mirroring insertHyperlink; validation incl. the 64-char tag cap; serializer escaping covers the tag) + pro `insertCustomNode(editor, def, attrs, text, {alias, lock, at})` — sdtLocked default, one undo unit, save/reopen recognized. REMAINING: rich-content `toDocx` ctx builders (hyperlink content needs `sanitizeHref`), customXml data-part hatch
- [x] 4.5a Per-line boundary fragments: inline-control geometry now groups placed spans per LINE (document-order line ordinal in the placed-span index), one fragment per line a control touches — a wrapped control no longer claims the words beside it; pinned by layout test
- [x] 4.11 Sidebar furniture v1: `DocxEditorReview` gains a `furniture` prop rendered at the top of the rail (`.docx-review__furniture`); per-item custom CARDS stay 4.10
- [ ] 4.12 Typed attr schemas on `defineCustomNode` (owner request 2026-08-05): accept a Standard-Schema validator (zod/valibot) for attrs so recognition/insert validate and `onClick` handlers receive typed data for richer detail popups
- [ ] 4.5 Core render contract: host furniture element (`createElement`, `contenteditable=false`, data attrs) + extent (fixed or text-equivalent) fed to layout/`TextMeasurer`; explicit invalidation API, no observed reflow
- [~] 4.6 Atomic offsets, first half: typing at a control's outer edge no longer joins the run inside it (`leavesInlineContainer`, now shared between apply and VALIDATE so a locked chip cannot refuse a keystroke that lands beside it); paragraph-END edge inserts a fresh run after the container (was: wrong run, or refused outright under `sdtContentLocked`); left-edge typing lands before the chip; `w:temporary` keeps eager boundary attribution. Whole-SDT backspace/delete DONE (owner request 2026-08-05): `inlineControlEndingAt`/`StartingAt` in the segment model; the surface's Backspace/Delete at a chip edge issues `removeContentControl {mode:'delete'}` (new op mode — wrapper AND content as one unit; wrapper lock refuses); insert default relaxed `sdtContentLocked`→`contentLocked` so nodes stay deletable (matching Word's lock semantics); pro `updateCustomNode` (remove+reinsert at the node's own span, one transaction) and `removeCustomNode`; context-menu "Remove {label}" row; demo Edit opens a PREFILLED form persisting via updateCustomNode. All pinned by tests. REMAINING: caret skip-as-unit (arrow keys), copy/paste carries OOXML, suggest-mode tracked deletion of a chip (currently untracked structural)
- [x] 4.6b Boundary-rect vertical offset under non-single line spacing: placed spans drop the line's leading (glyphs are bottom-anchored in the line box), so fragments sit on the text; the `CustomNodeChrome` downward hit-test fallback is removed — clicks hit the rect that is painted. Also resolved en route: the deferred structural-balloon assertion in `review-sidebar.test.tsx` was a STALE `packages/react/dist` (pro tests paint through the built adapter, which inlines the core painter, and the bundle predated the #125 row-attribution datasets) — rebuilt, assertion enabled
- [x] 4.7 Pro-level interaction + appearance API: `CustomNodeChrome` (pro/react) — definition-driven `chrome.color` (borderless tint), `onClick`/`onHover` on definitions plus component-level `onNodeClick`/`onNodeHover` with `ActivatedCustomNode` (attrs + rect). Engine-layer dispatch remains the deeper follow-up
- [ ] 4.8 React portal sugar: mount JSX renders into host elements at measured extent
- [x] 4.10 `reviewCard` hook: `kind: 'custom'` review items derived from recognized nodes (`customItemsOf` in the pro review model, registry definitions forwarded opaquely through `ReviewModelInput.customNodes`), anchored at the node's range via `locateSites`; the pane renders the card (title/detail, informational, no accept/reject/reply — engine refuses the verbs). REMAINING: a host-supplied per-item card RENDERER beyond the part-override mechanism (owner FYI 2026-08-05)
- [x] 4.10b Context-menu section (owner request 2026-08-05): `CustomNodeContextMenu` (pro/react, `docxRowPlacement: 'start'`) renders the pointed-at node's card data plus an "Edit {label}" row (`onEdit` definition hook) ABOVE the packaged rows; react context menu now captures the opening right-click target (`useContextMenuTarget`). Interaction lives on right-click ON PURPOSE — a left click competes with the caret. `insertCustomNode` now defaults to `sdtContentLocked`, so the label only changes through the edit flow (persistence API for text+attrs lands with 4.12 schemas)
- [x] 4.10d Reviewer round (3-agent panel, 2026-08-05): CRITICAL fixed — validation's collapsed-caret attribution now mirrors apply's FULL rule (bias + deleted-segment state + leavesInlineContainer, threaded through `contentControlAtCaret`), closing a `bias:'right'` lock bypass that wrote inside `sdtContentLocked` chips; the beside-insert lands after the inner container within its PARENT (nested controls stay inside the outer one); `reviewItemsAt` tie-break is a total order (comment < custom < revision); custom cards band via the comment band only while active. Security lows: chip CSS selectors are exact-or-query-prefix (no `^=` over-match spoofing) and identity-charset guarded (`CUSTOM_NODE_IDENTITY_PATTERN`, enforced by defineCustomNode + re-checked at raw-object entry points); `customNodeXml` strips XML-invalid control chars. DX blockers: `ActivatedCustomNode` carries post-`fromDocx` attrs + `nodeId`/`text` (one shape across click/hover/edit/cards via `activatedCustomNodeOf`); `ReviewItemPlacement` is a DISCRIMINATED union; honest `onEdit` TSDoc (no update call yet). KNOWN GAP: keyboard-opened menus have no pointer target, so the custom section needs a caret fallback (divergence note)
- [x] 4.10c DX round (owner review 2026-08-05): chrome components default to the REGISTERED definitions (`Editor.getCustomNodeDefinitions`, opaque; narrowed by the shared `isCustomNodeDefinition` guard) — register once, every surface follows; `useReviewItem` (pro/react, public) lets host children inside rail cards read the current item (demo: per-card button); `customNodeSdtXml` builds the same locked, tagged `w:sdt` markup as a STRING for server-side/other-tool authoring, recognition-equivalence pinned by test; demo gained an insert-citation form dialog in front of `insertCustomNode`
- [x] 4.9b Word Online survival (owner report 2026-08-05): a chip authored WITHOUT `w:id` is silently DELETED by Word Online on resave (the surviving fixture carries one; Word writes it on every control). Both write paths now emit a deterministic `w:id` (FNV-1a; minted-node-id+tag in the op, tag+text in `customNodeXml` with an override option), placed per CT_SdtPr's schema SEQUENCE — alias, tag, id, lock — which the deep validator enforces (id-first, Word's own resave order, is refused as known-node-invariant)
- [x] 4.9 Tests: Word-round-trip fixtures `e2e/fixtures/sdt-custom-tag-original.docx` / `sdt-custom-tag-word-roundtrip.docx` (recognition identical on both — GREEN), codec overflow + proto-pollution guards, unregistered-prefix literalness, fromDocx veto. REMAINING with 4.4-4.8: sanitizeHref fixture, pagination chip, atomic deletion

## 5. Surface bookkeeping

- [x] 5.1 Parity: pro integration points documented in intentional-export-divergence; `check:export-parity`, `check:parity-contract`, `check:adapter-css-thin` green (`check:public-docs-surface` red pre-existing on this branch)
- [x] 5.2 API Extractor: react/vue snapshots re-extracted, `api:check` green. Pro snapshot dir deferred until pro gets a build (extractor runs over dist)
- [x] 5.3 CSS: pane keeps consuming the core stylesheet classes (no new CSS in pro); `check:adapter-css-thin` green
- [~] 5.4 Changeset added (`pro-package-split.md`, minor). REMAINING: docs-site word-features matrix + MDX for the free/pro split before this ships
- [x] 5.5 Full gates: `bun run typecheck`, `bun test` vs baseline, `bun run i18n:validate`, `openspec validate pro-review-and-custom-nodes --strict`
