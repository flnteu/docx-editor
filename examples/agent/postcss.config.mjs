// Empty on purpose. The shipped stylesheet is already compiled and scoped, so
// this example needs no PostCSS pass. Declaring the file anyway stops the
// bundler walking up into the monorepo's own config.
export default { plugins: {} };
